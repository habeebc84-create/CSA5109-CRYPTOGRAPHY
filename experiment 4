text = input("Enter text: ").upper()
key = input("Enter key: ").upper()

result = ""

for i in range(len(text)):
    x = ord(text[i]) - 65
    y = ord(key[i % len(key)]) - 65
    result += chr((x + y) % 26 + 65)

print("Encrypted Text:", result)
