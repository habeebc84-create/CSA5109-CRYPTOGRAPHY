text = input("Enter text: ").upper().replace(" ", "")

key = list(map(int, input("Enter key values: ").split()))

cipher = ""

for i in range(len(text)):
    x = ord(text[i]) - 65
    cipher += chr((x + key[i]) % 26 + 65)

print("Ciphertext:", cipher)
